export interface ITheme {
  avatar?: {
    border_radius: string
  },
  name?: {
    color: string
  },
  bio?: {
    color: string
  }
  links?: {
    border_radius: string,
    background: string,
    color: string
  }
}
